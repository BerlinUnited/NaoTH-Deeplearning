# we use yolo2026 model in different sizes
mlflow_experiment_name: "FIFA-Mini-Ball-TOP"
modelsize: "n" # n, m, l, x
target_class: "Ball"
camera: "TOP" # BOTTOM or TOP
# optional
event_ids:
  - 10 # GO26

epochs: 8000
split_ratio: 0.8 # default is 0.8
