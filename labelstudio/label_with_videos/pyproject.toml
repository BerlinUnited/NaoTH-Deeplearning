[project]
name = "label-with-videos"
version = "0.1.0"
description = "Toolchain to label images via video annotation in LabelStudio"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "label-studio-sdk>=2.0.20",
    "numpy>=1.26.0",
    "opencv-python>=4.8.0",
    "requests>=2.31.0",
]
